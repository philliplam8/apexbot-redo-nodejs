{
  "name": "protractor",
  "version": "1.0.0",
  "description": "",
  "main": "config.js",
  "scripts": {
    "setup": "npm i && npm run webdriver-start",
    "prewebdriver-start": "webdriver-manager update",
    "webdriver-start": "webdriver-manager start",
    "protractor": "protractor config.js"
  },
  "author": "Phillip Lam",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "protractor": "^7.0.0"
  },
  "devDependencies": {
    "jasmine-spec-reporter": "^7.0.0"
  }
}
